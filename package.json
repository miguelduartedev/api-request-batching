{
  "targets": {
    "modern": {
      "engines": {
        "browsers": ">= 50%"
      }
    }
  },
  "engines": {
    "node": ">=18.14.2"
  },
  "scripts": {
    "start": "npx parcel ./src/index.html",
    "build": "npm i && npx parcel build ./src/index.html",
    "test": "npx jest",
    "clean": "rm -rf node_modules && rm -rf .parcel-cache && rm -rf dist"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "parcel": "^2.9.3",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@babel/core": "^7.22.5",
    "@babel/preset-env": "^7.22.5",
    "@babel/preset-typescript": "^7.22.5",
    "@types/jest": "^29.5.2",
    "axios": "^1.4.0",
    "babel-jest": "^29.5.0",
    "buffer": "^6.0.3",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.1"
  }
}
